<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @blog, local: true) do |form| %>
  <%= form.hidden_field :title %>
  <%= form.hidden_field :content %>

  <p>タイトル：<%= @blog.content %></p>
  <p>内容：<%= @blog.content %></p>

  <%= form.submit "登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
